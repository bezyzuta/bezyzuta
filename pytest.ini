[pytest]
# Only collect the real unit-test suite. Keeps pytest from trying to import
# the optional automation/ scripts (e.g. automation/snip_test.py matches the
# default *_test.py pattern), which raise SystemExit when their optional deps
# (playwright/pyautogui) aren't installed.
testpaths = tests
